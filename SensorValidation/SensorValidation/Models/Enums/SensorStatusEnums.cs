namespace SensorValidation.Models
{
    public enum SensorStatus
    {
        Idle = 1,
        Awaiting = 2,
        Updating = 3,
    }
    public enum SensorActivityStatus
    {
        Online = 1,
        Disconnected = 2,
        Error = 3,
    }

}
